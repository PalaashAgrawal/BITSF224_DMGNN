All code components have been written sequentially.
To run those components, all that needs to be done is running the notebook sequentially on Google Colab, using GPU as runtime.

Note: You may need to restart the notebook between the two tasks (Before GA implementation), if GPU memory Limit is exceeded.
